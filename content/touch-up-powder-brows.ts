/** Standalone treatment detail page — Touch-up Powder Brows. Data lives in
 *  touch-up-powder-brows.json (CMS + AI editable) and renders at /touch-up-powder-brows. */
import type { DetailContent } from '@/lib/types'

import data from './touch-up-powder-brows.json'

export const touchUpPowderBrows = data as unknown as DetailContent
